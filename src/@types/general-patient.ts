export interface GeneralPatientProps {
  id: string;
  name: string;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  status: any;
}

export interface GeneralAppointmentProps {
  id: string;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  status: any;
}
